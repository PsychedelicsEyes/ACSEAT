{% extends 'base.html.twig' %}
{% block body %}


    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.price) }}
    {{ form_row(form.category) }}
    <button type="submit">Modifié le produit</button>
    {{ form_end(form) }}


{% endblock %}